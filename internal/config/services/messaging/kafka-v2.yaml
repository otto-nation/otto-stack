name: kafka
description: Complete Apache Kafka messaging platform with UI and topic management
type: container
runtime:
  container:
    restart: unless-stopped
integration:
  dependencies:
    provides:
      - messaging
      - streaming
      - event-processing
documentation:
  examples:
    - kafka-console-producer --bootstrap-server ${KAFKA_HOST:-localhost}:${KAFKA_PORT:-9092} --topic test-topic
    - kafka-console-consumer --bootstrap-server ${KAFKA_HOST:-localhost}:${KAFKA_PORT:-9092} --topic test-topic --from-beginning
  usage_notes: Complete Kafka setup with Zookeeper coordination, broker, automatic topic creation, and web UI for management
  links:
    - https://kafka.apache.org/documentation/
    - https://github.com/provectus/kafka-ui
  use_cases:
    - Event streaming and processing
    - Message queuing and pub/sub
    - Real-time data pipelines
    - Microservices communication
    - Log aggregation and analytics
