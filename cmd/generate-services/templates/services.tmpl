// Code generated by generate-services. DO NOT EDIT.

package constants

// Service category constants
const (
{{range .Categories}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service type constants
const (
{{range .ServiceTypes}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service client constants
const (
{{range .Clients}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service port constants
const (
{{range .Ports}}	{{.Name}} = {{.Value}}
{{end}})

// Service name constants
const (
{{range .Names}}	{{.Name}} = "{{.Value}}"
{{end}})

// Default Docker image constants
const (
{{range .Images}}	{{.Name}} = "{{.Value}}"
{{end}})

// Default user constants
const (
{{range .DefaultUsers}}	{{.Name}} = "{{.Value}}"
{{end}})

// Connection flag constants
const (
{{range .ConnectionFlags}}	{{.Name}} = "{{.Value}}"
{{end}})

// Environment variable constants
const (
{{range .EnvVars}}	{{.Name}} = "{{.Value}}"
{{end}})

// Health check endpoint constants
const (
{{range .HealthEndpoints}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service tag constants
const (
{{range .Tags}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service dependency arrays
var (
{{range .Dependencies}}	{{.Name}} = {{.Value}}
{{end}})

// Service conflict arrays
var (
{{range .Conflicts}}	{{.Name}} = {{.Value}}
{{end}})

// Service provides arrays
var (
{{range .Provides}}	{{.Name}} = {{.Value}}
{{end}})
