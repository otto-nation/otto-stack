// Code generated by generate-services. DO NOT EDIT.
package services

import (
	"github.com/otto-nation/otto-stack/internal/core"
)

// YAML Structure Constants
const (
	// Top-level sections
	YAMLKeyContainer = "container"
	YAMLKeyService   = "service"
	
	// Container section
	YAMLKeyImage       = "image"
	YAMLKeyEnvironment = "environment"
	YAMLKeyPorts       = "ports"
	YAMLKeyVolumes     = "volumes"
	YAMLKeyRestart     = "restart"
	YAMLKeyCommand     = "command"
	YAMLKeyNetworks    = "networks"
	YAMLKeyMemoryLimit = "memory_limit"
	YAMLKeyHealthCheck = "health_check"
	
	// Service section
	YAMLKeyConnection   = "connection"
	YAMLKeyDependencies = "dependencies"
	YAMLKeyManagement   = "management"
	
	// Connection section
	YAMLKeyType        = "type"
	YAMLKeyDefaultPort = "default_port"
	YAMLKeyDefaultUser = "default_user"
	YAMLKeyClient      = "client"
	YAMLKeyHostFlag    = "host_flag"
	YAMLKeyPortFlag    = "port_flag"
	YAMLKeyUserFlag    = "user_flag"
	YAMLKeyDBFlag      = "database_flag"
	
	// Dependencies section
	YAMLKeyRequired  = "required"
	YAMLKeySoft      = "soft"
	YAMLKeyConflicts = "conflicts"
	YAMLKeyProvides  = "provides"
	
	// Port/Volume structure
	YAMLKeyExternal    = "external"
	YAMLKeyInternal    = "internal"
	YAMLKeyName        = "name"
	YAMLKeyMount       = "mount"
	YAMLKeyDescription = "description"
	
	// Service metadata
	YAMLKeyServiceType = "service_type"
	YAMLKeyHidden      = "hidden"
)

// Service Categories
const (
{{range .Categories}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service Clients
const (
{{range .Clients}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service Ports
const (
{{range .Ports}}	{{.Name}} = {{.Value}}
{{end}})

// Service Names
const (
{{range .Names}}	{{.Name}} = "{{.Value}}"
{{end}})

// Docker Images
const (
{{range .Images}}	{{.Name}} = "{{.Value}}"
{{end}})

// Default Users
const (
{{range .DefaultUsers}}	{{.Name}} = "{{.Value}}"
{{end}})

// Connection Flags
const (
{{range .ConnectionFlags}}	{{.Name}} = "{{.Value}}"
{{end}})

// Environment Variables
const (
{{range .EnvVars}}{{if eq .Name "EnvLocalstackAWS_DEFAULT_REGION"}}	{{.Name}} = core.DefaultAWSRegion
{{else}}	{{.Name}} = "{{.Value}}"
{{end}}{{end}})

// Environment Variable Keys
const (
{{range .EnvKeys}}	{{.Name}} = "{{.Value}}"
{{end}})

// Health Endpoints
const (
{{range .HealthEndpoints}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service Tags
const (
{{range .Tags}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service Capabilities
const (
{{range .Capabilities}}	{{.Name}} = "{{.Value}}"
{{end}})

// Networks
const (
{{range .Networks}}	{{.Name}} = "{{.Value}}"
{{end}})

// Memory Limits
const (
{{range .MemoryLimits}}	{{.Name}} = "{{.Value}}"
{{end}})

// Protocols
const (
{{range .Protocols}}	{{.Name}} = "{{.Value}}"
{{end}})

// Service Characteristics Map
var ServiceCharacteristics = map[string][]string{
{{range .Services}}{{if .Characteristics}}	"{{.Name}}": {{"{"}}{{range $i, $char := .Characteristics}}{{if $i}}, {{end}}"{{$char}}"{{end}}{{"}"}},
{{end}}{{end}}}
